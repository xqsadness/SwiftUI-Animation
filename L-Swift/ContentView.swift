//
//  ContentView.swift
//  L-Swift
//
//  Created by darktech4 on 02/10/2023.
//

import SwiftUI

struct ContentView: View {
    @State var show = true
    
    var body: some View {
        VStack{
            /*
             //Lazy Compositional Layout - Pagination - JSON Parsing Async/Await
             HomeCompositional()
             
             //Particle Effects - Animations
             ParticleEffectsHome()
             
             //Tabbar2
             HomeTabbar2View()
             
             //HomeOnBoarding2
             HomeOnBoarding2()
             
             //HomeOnboarding
             OnboardingView()
             
             //Side menu
             SideMenuView(show: $show)
             
             // Movable Grids in 5 Mins - Drag & Drop
             MovableGrids()
             
             //Arc Menu Button
             ArcMenuButton(buttons: Array(ArcMenuButtonName.allCases)) { button in
             switch button {
             case .capture:
             print("Capture button tapped.")
             case .star:
             print("star button tapped.")
             case .noti:
             print("noti button tapped.")
             case .bookmark:
             print("bookmark button tapped.")
             }
             }
             
             //Strength Meter with Progress & Color Feedback
             PasswordStrength()
             
             */
        }
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
